# Retired: upstream split images and credentials are intentionally unused.
name: Retired upstream nightly
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: printf '%s\n' 'Use Container CI and edge; upstream nightly publishing is retired.'
